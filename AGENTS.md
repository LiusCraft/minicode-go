# AGENTS.md

## 使用指南

### 语言约定
- **必须使用中文回答用户的所有问题**
- 代码注释除外（除非明确要求）

### 需求记录约定
- 当用户明确表达某件事“可以做但先留到后面”“优先级不高”“先记下来以后再做”等或其他类似含义时，应将其视为需要记录的 backlog feature
- 这类事项应优先记录到 `backlog/` 目录下对应的 markdown 文件中，而不只是停留在当前对话或 `README.md` 里
