# assets

This directory stores default static resources used by minioc.

- `assets/minioc.json`: default configuration template

At runtime, minioc creates `.minioc/minioc.json` from this template when the runtime config does not exist.
